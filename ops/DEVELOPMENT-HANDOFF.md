# دليل التسليم والاستمرارية

## الغرض

هذا الملف يمنع فقدان سياق المشروع عند انتهاء الحصة أو نقل المستودع إلى حساب آخر. يجب تحديثه في نهاية كل جلسة تنفيذية مؤثرة.

## الحالة الحالية

- نوع العمل: تنفيذ أساس تطبيق Flutter وربطه بالمصادقة الفعلية.
- المستودع الخارجي: `https://github.com/aaaaubad-byte/amansoon`.
- الفرع الرئيسي: `main`.
- آخر Commit مرحّل: `2afa16c` قبل هذا التحديث؛ تغييرات جلسة التطبيق لم تُرحّل بعد.
- مشروع Supabase: `amansoon`، Project Ref `yqsnnooupyhahohyyiho`.
- آخر مرحلة مكتملة: إنشاء الهجرة الأولى للجداول والعلاقات وسياسات RLS.
- المرحلة الحالية: تهيئة تطبيق Flutter وربطه بـ Supabase Auth — الأساس البرمجي منفذ، والتحقق النهائي ينتظر Flutter SDK.
- آخر تغيير: إضافة `aman_app` مع إعداد Supabase، تدفق التسجيل/الدخول، قراءة `profiles.role`، وواجهة عربية أولية.
- المرجع البصري: `brand/assets/aman-brand-reference.png`.

## قواعد العمل

1. ابدأ بقراءة `README.md` ثم `docs/ROADMAP.md`.
2. افحص آخر Commit وملفات التغيير قبل التعديل.
3. نفذ مرحلة واحدة أو جزءًا صغيرًا قابلًا للاختبار.
4. شغّل الاختبارات بعد كل تغيير جوهري.
5. لا تضع أسرارًا في الملفات أو Git.
6. حدّث هذا الملف وسجل القرارات قبل إنهاء الجلسة.
7. أنشئ Commit واضحًا بعد كل وحدة مكتملة.
8. لا تعتبر الميزة مكتملة اعتمادًا على شكل الواجهة فقط؛ يجب اختبار اتصالها بقاعدة البيانات الفعلية.

## قالب تحديث الجلسة

انسخ القالب التالي في نهاية كل جلسة أو حدّث قسم الحالة:

```text
التاريخ:
الحساب/البيئة:
المرحلة:
ما تم إنجازه:
الملفات المهمة:
الاختبارات التي نجحت:
المشكلات المفتوحة:
القرار التالي:
الأسرار أو إعدادات البيئة المطلوبة دون قيم سرية:
```

## خطوات نقل المشروع إلى حساب آخر

1. اربط الحساب الجديد بالمستودع.
2. نفذ `git clone` أو افتح المشروع من المستودع.
3. اقرأ `README.md` و`docs/PRODUCT-SPEC.md` و`docs/ARCHITECTURE.md` و`docs/ROADMAP.md`.
4. اقرأ آخر تحديث في هذا الملف.
5. أنشئ `.env` محليًا اعتمادًا على `.env.example`، ولا تنسخ قيم الأسرار من Git.
6. اربط التطبيق بمشروع Supabase تطويري مصرح به.
7. شغّل هجرات SQL وسياسات RLS الخاصة بالبيئة فقط.
8. نفذ الاختبارات قبل استئناف التطوير.
9. أكمل من أول بند غير مكتمل في خارطة الطريق.

## سياسة Git

استخدم رسائل Commit تصف النتيجة، مثل:

```text
feat(auth): add customer registration
feat(protection): approve request transactionally
feat(tasks): complete task and create next cycle
fix(tasks): prevent duplicate cycle creation
chore(docs): update development handoff
```

يفضل استخدام فروع قصيرة مثل `feature/auth` و`feature/protection-requests` و`fix/task-idempotency`. لا تدمج تغييرًا تجاريًا حساسًا قبل الاختبارات.

## متغيرات البيئة المتوقعة

لا تضع قيمًا حقيقية هنا. أسماء المتغيرات الأولية المقترحة:

```text
SUPABASE_URL=
SUPABASE_ANON_KEY=
# لا تضع SUPABASE_SERVICE_ROLE_KEY داخل APK أو Git.
```

## خطة النسخ الاحتياطي

قبل أي هجرة كبيرة أو تغيير بيانات في Staging/Production، يجب أخذ نسخة احتياطية والتحقق من إمكانية استرجاعها. لا تنفذ تغييرات تدميرية على البيانات التاريخية؛ استخدم تعطيلًا منطقيًا أو هجرات تحفظ السجل.

## آخر نقطة استئناف

نقطة الاستئناف التالية: توفير Flutter SDK وAndroid SDK، ثم تشغيل `flutter pub get` و`flutter test` و`flutter build apk` باستخدام متغيري `SUPABASE_URL` و`SUPABASE_ANON_KEY`. بعد نجاح البناء، اختبر التسجيل والدخول وقراءة الدور وRLS بمستخدم عميل ومستخدم مدير، ثم ابدأ واجهات الشركات والأرقام في المرحلة 2.

## تحديث جلسة 2026-09-19

- الحساب/البيئة: `aaaaubad-byte/amansoon`، Supabase `amansoon` (`yqsnnooupyhahohyyiho`).
- المرحلة: المرحلة 0، أساس التطبيق.
- ما تم إنجازه: إنشاء `pubspec.yaml`، إعدادات البيئة، عميل Supabase، مستودع المصادقة، الثيم العربي، شاشات Auth/Home، واختبار إعدادات بسيط.
- الملفات المهمة: `aman_app/lib/main.dart`، `aman_app/lib/data/supabase/repositories/auth_repository.dart`، `aman_app/README.md`.
- الاختبارات التي نجحت: `git diff --check`، فحص عدم وجود مفاتيح فعلية. لم تُشغل اختبارات Flutter لعدم توفر SDK.
- المشكلات المفتوحة: يلزم تثبيت Flutter وAndroid SDK، واختبار تدفق حقيقي بمستخدمين واختبار RLS.
- القرار التالي: تشغيل الاختبارات والبناء في بيئة Flutter، ثم إضافة تدفق الشركات والأرقام.
- الأسرار المطلوبة: `SUPABASE_URL` و`SUPABASE_ANON_KEY` فقط وقت التشغيل/البناء؛ يمنع `service_role` داخل التطبيق.

## تحديث متابعة 2026-09-19

- تم فحص سياسات RLS الفعلية في مشروع Supabase؛ شركات الاتصالات والباقات المرئية تقرأ للعملاء، وإضافة الأرقام مقيدة بـ `customer_id = auth.uid()`.
- أضيفت نماذج `TelecomCompany` و`ProtectionPackage` و`CustomerNumber`.
- أضيف `CatalogRepository` لقراءة الشركات والباقات وأرقام المستخدم وإضافة رقم جديد عبر Supabase.
- أضيف اختبار تحويل صفوف قاعدة البيانات إلى نماذج Dart.
- لم تُنفذ اختبارات Flutter الفعلية بعد لعدم توفر Flutter SDK في البيئة.

## تحديث واجهة العميل 2026-09-19

- ربط شاشة العميل بقراءة أرقام المستخدم من Supabase.
- إضافة شاشة `أرقامي` مع تحديث بالسحب، حالات التحميل والخطأ والحالة الفارغة.
- إضافة نموذج إدخال رقم مع اختيار شركة فعالة ومرئية من قاعدة البيانات.
- معالجة أخطاء التكرار والصلاحية القادمة من PostgreSQL دون عرض نجاح وهمي.
- لا توجد شركات مدخلة حاليًا في قاعدة amansoon، لذلك ستظهر حالة عدم توفر الشركات حتى يضيفها المدير.

## لوحة المدير 2026-09-19

- أضيفت `AdminDashboard` مع تبويبين للشركات والباقات.
- يمكن للمدير إضافة وتعديل وتفعيل وتعطيل الشركات والباقات عبر Supabase.
- التعطيل منطقي باستخدام `status = inactive`، دون حذف السجلات التاريخية.
- طبقة `CatalogRepository` أصبحت تدعم عمليات الإدارة بالإضافة إلى قراءة العميل.
- الوصول الإداري يعتمد على RLS الفعلية، ولا تستخدم الشاشة `service_role`.
- لم تُشغل اختبارات Flutter أو البناء بسبب عدم توفر Flutter SDK.

## وحدة طلب الحماية 2026-09-19

- أضيفت نماذج `PaymentMethod` و`ProtectionRequest`.
- أضيف `ProtectionRepository` لقراءة وسائل الدفع وإنشاء طلب حماية للعميل.
- أضيف معالج من أربع خطوات لاختيار الرقم والباقة ووسيلة الدفع ورقم المرجع.
- تحفظ قيمة ومدة الباقة داخل الطلب كـ snapshot.
- أضيف اختبار تحويل صفوف الدفع والطلبات إلى نماذج Dart.
- المرحلة التالية: شاشة قائمة طلبات العميل، ثم مراجعة المدير والقبول/الرفض عبر RPC معاملية.

## مراجعة الطلبات 2026-09-19

- أضيفت دالتا PostgreSQL `approve_protection_request` و`reject_protection_request` بصلاحية `SECURITY DEFINER` والتحقق من `is_admin()`.
- القبول ينفذ تحديث الطلب وإنشاء الحماية والمهمة الأولى وتحديث الرقم وسجل التدقيق والإشعار داخل معاملة واحدة.
- الرفض يتحقق من سبب غير فارغ ويسجل الحدث والإشعار دون إنشاء حماية أو مهمة.
- أضيفت شاشة المدير لطلبات المراجعة مع قبول ورفض وسبب إلزامي.
- تم التحقق من منح EXECUTE للمستخدم `authenticated` فقط، مع منع PUBLIC.

## قوائم الحماية 2026-09-19

- أضيفت نماذج `Protection` و`OperationalTask`.
- أضيفت قراءة طلبات العميل وحماياته إلى `ProtectionRepository`.
- أضيفت شاشة `طلباتي وحماياتي` للعميل مع حالات الطلب وتاريخ انتهاء الحماية.
- لم تعرض مهام التشغيل للعميل لأن سياسة RLS الحالية تقصر جدول `operational_tasks` على المدير.
- الخطوة التالية: شاشة مهام المدير وRPC إكمال المهمة وإنشاء الدورة التالية بشكل idempotent.

## المهام التشغيلية 2026-09-19

- أضيفت RPC `complete_operational_task` مع تحقق المدير وقفل الصف.
- إكمال المهمة يكتب المنفذ والوقت، وينشئ دورة تالية واحدة فقط حسب إعدادات الشركة.
- عند غياب إعداد الشركة، يحتفظ النظام بمبلغ المهمة الحالية ويستخدم تكرارًا افتراضيًا قدره 30 يومًا.
- أضيفت شاشة مهام المدير مع حالات الاستحقاق وزر إكمال وتحديث القائمة.
- تم التحقق من تطبيق RPC في مشروع Supabase؛ اختبارات Flutter والبناء ما زالت معلقة بسبب عدم توفر SDK.

## إعدادات المهام 2026-09-19

- أضيف نموذج `TaskSettings` ومستودع `TaskSettingsRepository`.
- أضيفت شاشة مدير لحفظ مبلغ المهمة والتكرار وقاعدة الاستحقاق وأيام التنبيه لكل شركة.
- الحفظ يستخدم `upsert` على قيد الشركة الفريد، ولا يغير قيم المهام التاريخية الموجودة.
- رُبطت الشاشة بلوحة المدير، مع استمرار اعتماد RPC إكمال المهمة على إعدادات الشركة.

## وسائل الدفع 2026-09-19

- أضيف `PaymentMethodsRepository` لعمليات الإدارة على وسائل الدفع.
- أضيفت شاشة المدير لإضافة وتعديل وتفعيل وتعطيل المحافظ والبنوك والصرافات.
- التعطيل يستخدم `status = inactive` حتى لا تتأثر الطلبات التاريخية.
- تم التحقق من أن سياسة `payment_methods_admin_write` تقصر الكتابة على المدير، وأن العميل يقرأ الفعال والمرئي فقط.

## إشعارات العميل 2026-09-19

- تم التأكد من مطابقة مستودع GitHub `aaaaubad-byte/amansoon` مع مشروع Supabase النشط `amansoon` ذي المرجع `yqsnnooupyhahohyyiho`.
- أضيف نموذج `AmanNotification` ومستودع `NotificationRepository` لقراءة إشعارات المستخدم وتحديد الإشعار كمقروء مع تقييد `recipient_id` بالمستخدم الحالي.
- أضيفت شاشة عربية للإشعارات بحالات التحميل، الخطأ، القائمة الفارغة، التحديث بالسحب، وتحديد المقروء عند فتح الإشعار.
- أضيف زر الإشعارات إلى لوحة العميل واختبار تحويل صف قاعدة البيانات إلى النموذج.
- لم تُشغل اختبارات Flutter لأن Flutter/Dart SDK غير متوفرين في البيئة؛ يلزم تشغيلها عند توفير SDK.

## سجل العمليات الإداري 2026-09-19

- أضيف نموذج `AuditLog` ومستودع `AuditLogRepository` لقراءة أحدث العمليات من جدول `audit_logs`.
- أضيفت شاشة `AuditLogsPage` وربطت بزر داخل لوحة المدير لعرض نوع العملية ومعرف الكيان والتاريخ.
- تم التحقق من سياسة Supabase الفعلية `audit_admin_access`: القراءة متاحة للمستخدمين المصادق عليهم فقط عند تحقق `is_admin()`.
- أضيف اختبار تحويل صف سجل العملية مع بيانات JSON.
- لم تُشغل اختبارات Flutter أو البناء لعدم توفر Flutter/Dart SDK في البيئة.

## قائمة حمايات المدير 2026-09-19

- أضيف `listAdminProtections` إلى `ProtectionRepository` مع الحقول التاريخية وبيانات العميل والشركة.
- أضيفت شاشة `AdminProtectionsPage` وربطت بلوحة المدير لعرض الحمايات والحالة والقيمة والمدة وتاريخ الانتهاء.
- تم التحقق من سياسة Supabase `protections_customer_access`: العميل يقرأ سجلاته فقط، والمدير يقرأ السجلات الإدارية.
- وُسّع اختبار نموذج الحماية للتحقق من حقول العميل والشركة.
- لم تُشغل اختبارات Flutter أو البناء لعدم توفر Flutter/Dart SDK في البيئة.
